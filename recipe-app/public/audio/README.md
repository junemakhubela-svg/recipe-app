# Audio

Spoken walkthroughs go here as MP3, named to match the `audioUrl` field in
`src/data/recipesData.js` (for example `shakshuka.mp3`).

`cooking-tips.mp3` is also required — the Home dashboard's tips guide points
at it directly.
