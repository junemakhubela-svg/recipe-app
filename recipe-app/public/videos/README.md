# Videos

Technique clips go here as MP4, named to match the `videoUrl` field in
`src/data/recipesData.js` (for example `shakshuka.mp4`).

Add at least one real clip before capturing the recipe-detail screenshot —
without a file the `<video>` element renders its fallback message rather than
a player.
